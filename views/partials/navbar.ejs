<!--     FileName: navbar.ejs      -->
<!--     Author: Dixit Hihoriya   -->
<!--     StudentID: 301201312     -->
<!--     Date : 19th June 2022     -->

<div class="header clearfix">
  <nav class="navbar navbar-expand-lg navbar-expand-sm navbar-dark bg-dark">
    <div class="navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto">
        <a href="/home" class="navbar-brand">
          <!-- Logo Image -->
          <img src="/images/logo.png" width="35" alt="" class="d-inline-block align-middle mr-2">
        </a>
        <% if(title=="Home" ){ %>
          <li class="nav-item active">
            <% } else { %>
          <li class="nav-item">
            <% } %>
              <a class="nav-link" aria-current="page" href="/home"><i class="fas fa-lg fa-house-user"></i> Home</a>
          </li>
          <% if(title=="About Me" ){ %>
            <li class="nav-item active">
              <% } else { %>
            <li class="nav-item">
              <% } %>
                <a class="nav-link" aria-current="page" href="/about"><i class="fas fa-lg fa-address-card"></i> About
                  Me</a>
            </li>

            <% if(title=="Projects" ){ %>
              <li class="nav-item active">
                <% } else { %>
              <li class="nav-item">
                <% } %>
                  <a class="nav-link" aria-current="page" href="/projects"><i class="fas fa-lg fa-toolbox"></i>
                    Projects</a>
              </li>

              <% if(title=="Services" ){ %>
                <li class="nav-item active">
                  <% } else { %>
                <li class="nav-item">
                  <% } %>
                    <a class="nav-link " aria-current="page" href="/services"><i class="fa-lg fa-brands fa-buffer"></i>
                      Services</a>
                </li>

                <% if(title=="Contacts" ){ %>
                  <li class="nav-item active">
                    <% } else { %>
                  <li class="nav-item">
                    <% } %>
                      <a class="nav-link " aria-current="page" href="/contact"><i
                          class="fas fa-lg fa-envelope-open-text"></i> Contacts</a>
                  </li>
                  <% if(title=="Contact List" ){ %>
                    <li class="nav-item active">
                      <% } else { %>
                    <li class="nav-item">
                      <% } %>
                        <a class="nav-link " aria-current="page" href="/contactList"><i
                            class="fas fa-address-book"></i></i> Contact List</a>
                    </li>

                    <% if(typeof displayName=='string' && displayName) { %>
                      <li class="nav-item active">
                      <li class="nav-item">
                        <a class="nav-link " aria-current="page" href="/logout"><i class="fas fa-sign-in-alt"></i></i>
                          Logout</a>
                      </li>
                      <% } else { %>
                        <li class="nav-item active">
                        <li class="nav-item">
                          <a class="nav-link " aria-current="page" href="/login"><i class="fas fa-sign-in-alt"></i></i>
                            Login</a>
                        </li>
                        <% } %>
      </ul>
    </div>
  </nav>
</div>